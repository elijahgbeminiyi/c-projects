#include <stdio.h>

/**
* main - function that performs the increment operations
* Return: return 0
*/

int main(void)
{
	int x = 1;

	printf("x++ produces: %d\n", x++);
	printf("Now x contains: %d\n", x);

	return (0);
}
